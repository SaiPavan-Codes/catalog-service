services:
  db:
    image: postgres:18
    environment:
      # Initialize the database with a deterministic user/password
      # NOTE: if you change these values after the database has been created the
      #       password will not automatically update. You must remove the
      #       pgdata volume (docker compose down -v) to reinitialise the DB.
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: catalog_db
    ports:
      - "5431:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d catalog_db || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10

  app:
    build: .
    image: my-catalog-service
    depends_on:
      db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/catalog_db
      SPRING_DATASOURCE_USERNAME: user
      SPRING_DATASOURCE_PASSWORD: pass
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_SHOW_SQL: "true"
    ports:
      - "8080:8080"

volumes:
  pgdata: {}
